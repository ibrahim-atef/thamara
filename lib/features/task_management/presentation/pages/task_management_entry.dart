import 'package:flutter/material.dart';
import 'task_management_page.dart';

class TaskManagementEntry extends StatelessWidget {
  const TaskManagementEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const TaskManagementPage();
  }
}